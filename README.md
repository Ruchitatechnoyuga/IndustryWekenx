
  # Convert design to Figma

  This is a code bundle for Convert design to Figma. The original project is available at https://www.figma.com/design/pfmTk3d03ibwEF3V5aE2mi/Convert-design-to-Figma.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  